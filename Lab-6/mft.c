#include <stdio.h>
#include <unistd.h>
#include <stdbool.h>
#include <string.h>
int main()
{
    int total_memory,n,i;
    printf("Enter total memory size\n");
    scanf("%d",&total_memory);

    printf("Enter the number of partitions\n");
    scanf("%d",&n);

    for(i=0;i<n;i++)
    {
        printf("Enter memory partiton %d\n",i+1);
        scanf("%d"partition[i]);
        tot+=partition[i];
    }
    external_fragment = total_memory-tot;


    for(i=0;i<m;i++)
    {
        if(partiton[i])
    }
}
